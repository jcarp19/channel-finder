import logo from "../images/logo.png";

const Header = () => {
    return (
        <header>
            <img src={logo} alt="Spectrum Channel Finder" className="logo" />
        </header>
    )
}
export default Header;